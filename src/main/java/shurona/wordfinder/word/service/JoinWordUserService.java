package shurona.wordfinder.word.service;

import shurona.wordfinder.word.JoinWordUser;

public class JoinWordUserService {
    // 유저가 입력한 단어 생성
    JoinWordUser generate(Long userId, String word) {

        // 이미 단어가 존재하는 지 확인 한다.

        JoinWordUser app = new JoinWordUser();
        return app;
    }

    // 유저가 입력간 단어 목록 갖고 오기

}
